# React Redux Testing

Install all dependencies:

```
npm install
```

Run the development frontend server:

```
npm start
```

Run the tests using:

```
npm run test
```

To watch for changes in the tests, use:

```
npm run test:watch
```
